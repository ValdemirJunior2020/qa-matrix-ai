APP_ENV=production
HOST=127.0.0.1
PORT=8000
ADMIN_EMAIL=infojr.83@gmail.com
OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_MODEL=qwen3:4b-instruct
OLLAMA_EMBED_MODEL=embeddinggemma
# Production: replace with your real Netlify URL. Add localhost only when developing.
CORS_ORIGINS=https://YOUR-SITE.netlify.app,http://localhost:5173
MAX_UPLOAD_MB=20
REQUEST_TIMEOUT_SECONDS=120
# JWT_SECRET is optional. If omitted, the server creates a strong local secret in data/database/server.secret.
